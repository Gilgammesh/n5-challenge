import styled from 'styled-components'
import styling from '../../../../styles/styling'

const Title = styled.div`
  text-align: center;
  letter-spacing: 2px;
  color: ${styling.colors.title};
`

export default Title
